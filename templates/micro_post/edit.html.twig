{% extends "base.html.twig" %}
{% block title %}
	EDITAR
{% endblock %}

{% block body %}
	{{ form(form) }}


		{# {{ form_start(form) }}
										{{ form_errors(form) }}
								
										{{ form_row(form.title) }}
										{{ form_row(form.text) }}
								
										{{ form_row(form.submit, { 'label': 'Submit me' }) }}
										{{ form_end(form) }} #}

	{% endblock %}
